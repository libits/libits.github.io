---
title: golang开发工程师---面试经验
date: 2023-10-15 
categories: [面经]
tags: [golang面经]
pin: false

toc: true
comments: true
typora-root-url: ../../../libits.github.io
math: false
mermaid: true
---

##  1. go的高性能主要体现在哪些地方

- 垃圾回收机制：三色标记法（黑白灰【存活，已经标记；存活未标记；未被标记即为垃圾对象】），作用是为用户回收不用的对象对内存进行管理

- 编译器速度快

- GMP模型  内存占用少 （线程2M，goroutine：2kb）

##  2.关于聊天网站的gin框架设计流程和步骤（项目流程）

1.首先进行环境配置和日志配置

2.使用gorm连接数据库

3。对项目设置路由选择，对前端的信息进行自定义校验

- 创建路由，启动web服务
- 使用post、get等请求进行对资源的处理，执行用户登录、文件上传、视频通话等操作
- 配置Gorm完成对数据库的连接

##  3.区块链peer节点的设置

crypto-config.yaml文件

CA、template

## 4. slice和channel的底层原理

slice有{data len cap}

channel底层相当于一个环形队列

##  5.操作系统协程是用户态的么

答案：是的

##  6.  有1万个资源，go如何通过管道channel处理

定义缓冲区的管道 使用sync/waitgroup

##  $$$7.负载均衡

- 随机负载均衡
- 加权随机负载均衡
- 轮询负载均衡
- 加权负载均衡
- 加权平滑轮询负载均衡
- 一致性hash负载均衡

##  8. goroutine发生阻塞的原因

- 原子、互斥性、通道操作（读空的通道、在关闭的通道内写）
- 由于网络请求、IO
- 解决办法（GMP模型）：执行该G的M会与P解绑，而P则尝试与其它自由的M绑定，继续执行其它G。

##  9.go锁的模式

- 正常模式/饥饿模式
  - 正常模式：所有锁的goroutine按照先进先出等到
  - 饥饿模式：当一个 goroutine 等待锁时间超过 1 毫秒时，或者当前队列只剩下一个 goroutine 的时候，Mutex 切换到饥饿模式。
- 互斥锁 mutex
- 互斥锁底层模式：
- 读写锁 RWMutex

##  10.GMP模型中goroutine有哪几种状态

初始 执行 等待 就绪 终止

##  11.mysql引擎

- innoDB：聚簇，行锁，支持外键
- MyISAM：非聚簇，表锁
- Memory：表锁
- federated：可支持远程登录
- archive：行锁，可以压缩

##  $$$ 12.支持中间件

- 全局中间件`gin.handlefunc`，在路由配置的时候用过 

##  13. 进程间通信的几种方式

- 基于信号量
- 消息队列
- 高级管道

##  14.为什么共享内存就使得进程通信效率高

- 避免数据复制的开销
- 避免高频率的数据交换
- 增加进程之间读写数据的效率

##  15.一次完整的https请求是怎样的

1.客户端向服务器发起HTTPS的请求，连接到服务器的443端口；

2.服务器将非对称加密的公钥以证书的形式回传给客户端，证书⾥面包含了网站地址，加密公钥，以及证书的颁发机构。

3.客户端接受到该公钥后进行验证，就是验证步骤2中服务器返回的证书，如果有问题，则HTTPS请求无法继续；如果没有问题，则上述公钥是合格的（第一次HTTP请求）。客户端这个时候随机生成一个密钥，称为client key，客户端密钥(即对称加密的密钥)，用于对称加密数据的。使用前面的公钥对client key进行非对称加密；

4.进行二次HTTP请求，将加密之后的client key传递给服务器；

5.服务器使用私钥进行解密，得到client key（对称加密的密钥）（利用非对称加密传输对称加密的密钥），使用client key对数据进行对称加密；

6.将对称加密后的数据传递给客户端，客户端收到后使用对称解密，得到服务器发送的数据，完成第二次HTTP请求。

##  16. go等待协程完成的方式

sync/waitgroup

wg.Done

wg.Add

wg.wait

##  $$$17.slice和数组的区别

- s数组是值传递，slice是引用传递
- slice是地址类型，数组是值类型
- slice没有长度，数组有长度

##  $$$18.go的反射reflect

- 让程序在程序运行的时候对一些特殊字段进行操作。
- 序列化和反序列的时候：文本反序列化成文本数据类型
- TypeOf  ValueOf